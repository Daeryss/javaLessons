public class lesson43_Math {
    public static void main(String[] args) {
        double sqrt = Math.sqrt(2); // корень 1.4142...
        double cbrt = Math.cbrt(27.0); // кубический корень is 3.0
        double cube = Math.pow(2, 3); // 2 в степени 3
        double sin = Math.sin(Math.PI / 2); // sin90°  is 1.0
        double cos = Math.cos(Math.PI); // cos180° is -1.0
        double grad = Math.toRadians(30); // 0.5235  преобразует угол, измеренный в градусах, в угол, измеренный в радианах (приблизительно).
        double random = Math.random(); // случайное число от 0 до 1

        String x = "a v";


        System.out.println(x.toCharArray().length);
    }
}
